import {
  appVersion,
  currentPlatfom,
  getCurrentWorkingDirectory,
  getHomePath,
} from "./handlers";

export const app = {
  currentPlatfom,
  appVersion,
  getHomePath,
  getCurrentWorkingDirectory,
};
