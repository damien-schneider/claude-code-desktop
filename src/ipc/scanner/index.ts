import {
  scanProjects,
  scanDirectory,
  getCachedProjects,
  getDefaultPaths,
  clearCache,
  checkCacheValid,
} from './handlers';

export const scanner = {
  scanProjects,
  scanDirectory,
  getCachedProjects,
  getDefaultPaths,
  clearCache,
  checkCacheValid,
};
